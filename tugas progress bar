<!DOCTYPE html>
<html> 
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Loading Scrapbook</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: #fdf6f0;
      font-family: 'Comic Sans MS', cursive;
    }
    .loader {
      text-align: center;
    }
    .emoji {
      font-size: 50px;
      animation: bounce 1s infinite;
    }
    .name-text {
      font-family: 'Comic Sans MS', cursive;
      font-size: 30px;
      font-weight: bold;
      color: #444;
      margin: 25px 0;
    }
    .loading-text {
      margin-top: 10px;
      font-size: 20px;
      color: #777;
    }
    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-15px); }
    }
    .progress {
      margin-top: 20px;
      height: 20px;
      border-radius: 10px;
    }
  </style>
</head>
<body>
  <div class="loader">
    <div class="emoji">🚀</div>
    <div class="name-text">Ryu Russel</div>
    <div class="loading-text">Membuka scrapbook kenangan...</div>
    <div class="progress w-75 mx-auto">
      <div id="progressBar" 
           class="progress-bar progress-bar-striped progress-bar-animated bg-warning" 
           role="progressbar" style="width: 0%">0%</div>
    </div>
  </div>

  <script>
    // Progress bar logic
    let progress = 0;
    const progressBar = document.getElementById("progressBar");
    const duration = 7500; // total duration in ms
    const interval = 150;  // update interval in ms
    const step = 100 / (duration / interval);

    const timer = setInterval(() => {
      progress += step;
      if (progress >= 100) {
        progress = 100;
        clearInterval(timer);
        window.location.href = "home.html"; // redirect after done
      }
      progressBar.style.width = progress + "%";
      progressBar.innerText = Math.floor(progress) + "%";
    }, interval);

    // Emoji bouncing & changing
    const emojis = ['🚀', '🌑', '☀️', '✨', '🚩'];
    let currentEmojiIndex = 0;
    const emojiElement = document.querySelector('.emoji');

    setInterval(() => {
      currentEmojiIndex = (currentEmojiIndex + 1) % emojis.length;
      emojiElement.textContent = emojis[currentEmojiIndex];
    }, 1000);
  </script>
</body>
</html>
